import Brands from "@/Pages/Dashboard/Brands/Brands";
import React from "react";

export const metadata = {
  title: "Brands | ABC Computers",
};

const page = () => {
  return <Brands />;
};

export default page;
