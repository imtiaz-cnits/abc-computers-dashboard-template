import React from 'react';

export const metadata = {
    title: "Incomes | ABC Computers",
  };

const page = () => {
    return (
        <div>
            incomes
        </div>
    );
};

export default page;